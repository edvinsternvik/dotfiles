require("appearance")
require("environment_vars")
require("input")
require("keybinds")
require("monitors")
require("programs")
require("window_rules")

hl.config({
    debug = {
        disable_logs = false,
    }
})
